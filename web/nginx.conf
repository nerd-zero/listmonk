server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        # Resolved at request time (not config load) via the variable, so
        # this container still starts even if "api" isn't reachable yet or
        # isn't on the network at all -- only requests under /api/ fail.
        resolver 127.0.0.11 valid=30s;
        set $upstream http://api:8181;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
